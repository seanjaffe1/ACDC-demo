wandb_project: acdc-cardiac-diffusion
wandb_run_name: main-egnn-flow
wandb_entity: null  # set to your wandb username/team, or leave null to use default

graph_dir: data/graphs
out_dir: checkpoints/main
struct: lv

# Model
hidden_dim: 128
latent_dim: 64
n_encoder_layers: 4
n_flow_layers: 4
in_node_dim: 1

# Training
epochs: 200
batch_size: 8
lr: 3e-4
weight_decay: 1e-4
kl_weight: 0.001
