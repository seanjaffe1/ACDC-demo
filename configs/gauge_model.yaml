wandb_project: acdc-cardiac-diffusion
wandb_run_name: gauge-egnn-flow
wandb_entity: null  # set to your wandb username/team, or leave null to use default

graph_dir: data/graphs_curvature
out_dir: checkpoints/gauge
struct: lv

# Model
model_type: gauge
hidden_dim: 128
latent_dim: 64
n_encoder_layers: 4
n_flow_layers: 4
in_node_dim: 4
curvature_dim: 4

# Training
epochs: 200
batch_size: 8
lr: 0.0003
weight_decay: 0.0001
kl_weight: 0.001
